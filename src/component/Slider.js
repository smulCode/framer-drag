import {motion} from 'framer-motion'
import {useRef, useEffect, useState} from 'react'



const Slider = () => {
  return (<motion.h1 animate={{x:250}}>Slider</motion.h1>);
};

export default Slider;
